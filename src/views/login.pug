extends base

block content
    form.loginForm.metaForm(method="POST")
        div.inputBox
            label
            input(name="userId",type="text",placeholder="아이디")
        div.inputBox
            label
            input(name="password",type="password",placeholder="비밀번호")
        div.inputBox
            label
            input(type="submit",value="Login")